<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Server Action for opening the automatic entry wizard -->
    <record id="action_account_move_automatic_entry_wizard" model="ir.actions.server">
        <field name="name">Transfer Accounting Entries</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">
            action = records.action_open_automatic_entry_wizard()
        </field>
    </record>
</odoo>

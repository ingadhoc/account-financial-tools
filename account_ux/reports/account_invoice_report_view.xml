<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_invoice_line_report_tree" model="ir.ui.view">
        <field name="name">account.invoice.report.list</field>
        <field name="model">account.invoice.report</field>
        <field name="inherit_id" ref="account.account_invoice_report_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="decoration-info">state == 'draft'</attribute>
                <attribute name="decoration-muted">state == 'cancel'</attribute>
            </xpath>
            <field name="quantity" position="after">
                <field name="price_unit" optional="show"/>
                <field name="discount" optional="hide" groups="sale.group_discount_per_so_line"/>
                <field name="discount_amount" sum="Total" optional="hide" groups="sale.group_discount_per_so_line"/>
            </field>
            <field name="price_total" position="after">
                <field name="price_subtotal" position="move"/>
                <field name="total_cc"  optional="hide" sum="Total"/>
                <field name="currency_id" optional="hide"/>
                <field name="current_currency_id" optional="hide" column_invisible="True"/>
                <field name="company_currency_id" optional="hide" column_invisible="True"/>
            </field>
            </field>
    </record>

    <record id="view_account_invoice_report_search" model="ir.ui.view">
        <field name="name">account.invoice.report.search</field>
        <field name="model">account.invoice.report</field>
        <field name="inherit_id" ref="account.view_account_invoice_report_search"/>
        <field name="arch" type="xml">
            <filter name='user' position="after">
                <filter string="Journal Entry" name="groupby_move_id" context="{'group_by':'move_id'}"/>
                <filter string="Invoice Currency" name="groupby_currency_id" context="{'group_by':'currency_id'}"/>
            </filter>
        </field>
    </record>

    <record id="view_account_invoice_report_pivot" model="ir.ui.view">
        <field name="name">account.invoice.report.pivot</field>
        <field name="model">account.invoice.report</field>
        <field name="inherit_id" ref="account.view_account_invoice_report_pivot"/>
        <field name="arch" type="xml">
            <pivot position="attributes">
                <attribute name="disable_linking"></attribute>
            </pivot>
        </field>
    </record>

    <record id="account.action_account_invoice_report_all" model="ir.actions.act_window">
        <field name="view_mode">list,graph,pivot</field>
    </record>

</odoo>

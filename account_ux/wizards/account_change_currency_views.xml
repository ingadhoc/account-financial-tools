<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_change_currency" model="ir.ui.view">
        <field name="name">Change Currency</field>
        <field name="model">account.change.currency</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="currency_to_id"/>
                    <field name="conversion_date" digits="[12,12]" groups="!account_ux.group_restrict_change_currency_exchange" help="This value will be used to update the values of the amount without forcing the currency rate"/>
                    <field name="conversion_date" digits="[12,12]" groups="account_ux.group_restrict_change_currency_exchange" readonly="1"/>
                </group>
                <footer>
                    <button name="change_currency" string="Change Currency and Rate" type="object" class="oe_highlight"/>
                    or
                    <button string="Cancel" class="oe_link" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_account_change_currency" model="ir.actions.act_window">
        <field name="name">Change Currency</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">account.change.currency</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_account_change_currency"/>
        <field name="context">{}</field>
        <field name="target">new</field>
    </record>
</odoo>
